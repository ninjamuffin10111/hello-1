we have a name, by default name is 'world'
we have an option, by default option is empty

if exactly two arguments:
  if the second argument starts with '-'
    it is an option
  else
    it is a name

elif there are more than two arguments:
    second argument is an option
    the third argument is a name

create our message to the name

if option is -m
  print multiple colors message
elif option is -c
  print colored message
else
  print plain ol message

